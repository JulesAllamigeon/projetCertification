{% extends 'base.html.twig' %}

{% block body %}

        <div class="container">

            <h1 class="mt-3 mb-3">Prise de rendez-vous pour {{ app.user }}</h1>
            {{ form_start(form) }}

            <div class="d-flex">
                <div class="form-group col-md-6">
                    {{ form_row(form.motive) }}
                    {{ form_row(form.date) }}
                    {{ form_row(form.job) }}
                    {{ form_row(form.sports) }}
                    {{ form_row(form.smoker) }}
                    {{ form_row(form.medical_past) }}
                    {{ form_row(form.medication) }}
                    {{ form_row(form.allergies) }}
                    {{ form_row(form.sleep_schedule) }}



                    <div class="text-right">
                        <button type="submit" class="btn btn-outline-primary">Valider</button>
                    </div>

                </div>

                {{ form_end(form) }}

                <div class="col-md-6">
                    <img class="mt-5"  src="{{ asset('images/massage2.jpeg') }}" width="100%">
                    <div class="card">
                        <div class="card-body" style="color: #1b1e21">
                            LOREM IPSUM LOREM IPSUM LOREM IPSUM LOREM IPSUM LOREM IPSUM LOREM IPSUM LOREM IPSUM
                        </div>
                    </div>
                </div>
            </div>
        </div>






{% endblock %}
